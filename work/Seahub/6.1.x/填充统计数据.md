```
function(group_by, start_time, end_time):
    time_delta = end_time - start_time
    if group_by == 'day':
        len = time_dalta.get(day)
    elif group_by == 'hour':
        l_day = time_delta.getday()
        l_hour = time_delta.gethour()
        len = l_day * 24 + l_hour
    data = get_seafile_data()
    res_data = [ {} * len ]
    for i in data:
        if group_by == 'day':
            res_data[(i[data] - start_time).getday()] = i
        elif group_by == 'hour':
            res_data[i[data] - start_time).gethour()] = i
    for i in range(len):
        if res_data[i] == {}:
            res_data[i] = 'fill data'
    return res_data
```